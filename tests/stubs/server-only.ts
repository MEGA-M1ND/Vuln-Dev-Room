// Test stub for Next.js's `server-only` guard, which has no runtime module.
// Importing it in a client bundle is a build error in Next; under Vitest we
// exercise these modules directly in a node environment, so it is a no-op.
export {};
